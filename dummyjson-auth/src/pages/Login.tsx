import { Card } from "../components/ui/card";

const Login = () => {
  return (
    <Card  className="w-full sm:max-w-md flex justify-center   items-center bg-red-400 h-full"></Card>
  );
};

export default Login;
